# Model files

`scripts/download_assets.py` downloads the official Auto-AVSR visual checkpoint here.
Checkpoints are intentionally excluded from Git because the selected file is about 1 GB.

The exact source, size, checksum, training-data names, benchmark, and licensing note are
recorded in `manifest.json`.
