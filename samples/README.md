# Samples

`scripts/download_assets.py` creates `test.mp4` from PyTorch's public AVSR preprocessing demo asset.
The generated clip has no audio stream, so it is also a visual-only verification fixture.

